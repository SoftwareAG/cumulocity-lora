import { OnInit } from '@angular/core';
import { Export, Schedule, EmitterPayload, ActionType } from './export-schedules.interface';
import { ReportsService } from './reports.service';
import { BsModalService, BsModalRef } from 'ngx-bootstrap/modal';
import { CronService } from './cron.service';
import { IdReference } from '@c8y/client';
import { TranslateService } from '@ngx-translate/core';
import { UserService } from '@c8y/client';
export declare class ExportSchedulesComponent implements OnInit {
    private reportsService;
    private bsModalService;
    cronService: CronService;
    private translateService;
    private userService;
    exportId: IdReference;
    exp: Export;
    scheduleList: Schedule[];
    initialSchedule: Schedule;
    listClass: string;
    loadingStatus: any;
    sortType: string;
    sortReverse: boolean;
    isOpen: any;
    isFlipped: boolean;
    isEditMenuOpen: boolean;
    modalRef: BsModalRef;
    currentUserEmail: string;
    private _exportId;
    constructor(reportsService: ReportsService, bsModalService: BsModalService, cronService: CronService, translateService: TranslateService, userService: UserService);
    ngOnInit(): Promise<void>;
    getCurrentUserEmail(): Promise<string[]>;
    getScheduleList(withProgress: boolean): Promise<void>;
    addSchedule(): void;
    editSchedule(schedule: Schedule, event: any): void;
    duplicateSchedule(schedule: Schedule, event: any): void;
    openAddEditModal(exportId: IdReference, schedule: Schedule, actionType: ActionType): void;
    getMessageFromModal(payload: EmitterPayload): void;
    removeSchedule(schedule: Schedule, event: any): Promise<void>;
}
