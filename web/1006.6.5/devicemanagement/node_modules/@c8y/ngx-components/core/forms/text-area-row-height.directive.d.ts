import { ElementRef, Renderer2 } from '@angular/core';
export declare class TextAreaRowHeightDirective {
    private elem;
    private renderer;
    rows: number;
    heightPerRow: string;
    private minHeightPerRow;
    constructor(elem: ElementRef, renderer: Renderer2);
    ngAfterViewInit(): void;
    private setHeight;
}
