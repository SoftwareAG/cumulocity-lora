import { AbstractControl, Validator } from '@angular/forms';
export declare class MaxValidationDirective implements Validator {
    max: string;
    validate(control: AbstractControl): {
        [key: string]: any;
    } | null;
}
