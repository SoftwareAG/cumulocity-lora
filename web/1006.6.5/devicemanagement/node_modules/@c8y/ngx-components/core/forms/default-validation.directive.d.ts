import { AbstractControl, Validator } from '@angular/forms';
export declare class DefaultValidationDirective implements Validator {
    c8yDefaultValidation: string;
    validate(control: AbstractControl): {
        [key: string]: any;
    } | null;
}
