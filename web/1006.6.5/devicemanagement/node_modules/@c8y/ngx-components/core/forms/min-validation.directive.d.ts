import { AbstractControl, Validator } from '@angular/forms';
export declare class MinValidationDirective implements Validator {
    min: string;
    validate(control: AbstractControl): {
        [key: string]: any;
    } | null;
}
