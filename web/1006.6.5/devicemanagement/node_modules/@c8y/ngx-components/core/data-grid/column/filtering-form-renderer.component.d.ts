import { ChangeDetectorRef, Injector, OnChanges } from '@angular/core';
import { FilteringFormRendererSpec } from './filtering-form-renderer';
export declare class FilteringFormRendererComponent implements OnChanges {
    private injector;
    private cd;
    spec: FilteringFormRendererSpec;
    _spec: FilteringFormRendererSpec;
    rendererType: 'TEMPLATE' | 'COMPONENT';
    rendererInjector: Injector;
    defaultRendererFilterText: string;
    constructor(injector: Injector, cd: ChangeDetectorRef);
    ngOnChanges(): void;
}
