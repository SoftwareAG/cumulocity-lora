import { OptionsService } from '../common/options.service';
import { AppStateService } from '../common/ui-state.service';
import { DocLink, DocLinkFactory } from './docs.models';
import { ExtensionPoint } from '../common/public-api';
import { Observable } from 'rxjs';
import { Router } from '@angular/router';
export declare class DocsService implements ExtensionPoint<DocLink> {
    private options;
    private app;
    items$: Observable<DocLink[]>;
    constructor(options: OptionsService, app: AppStateService, factories: DocLinkFactory[], router: Router);
    getBaseUrl(): string;
    readonly templateStr: string;
    getUserGuideLink(link: any): any;
    list(): Promise<DocLink[]>;
    refresh(): void;
    get(): {
        url: any;
        icon: string;
        label: string;
        type: "doc" | "quicklink";
        click?: any;
    }[];
    private getLink;
    private prefixWithSlash;
}
