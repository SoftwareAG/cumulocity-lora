import { EventEmitter } from '@angular/core';
import { IUser, UserService } from '@c8y/client';
import { AlertService } from '../alert/public-api';
export declare class UserTotpRevokeComponent {
    private users;
    private alert;
    user: IUser;
    revokeEmitter: EventEmitter<undefined>;
    constructor(users: UserService, alert: AlertService);
    revoke(): Promise<void>;
}
