import { BasicAuth, FetchClient, IUser, UserService } from '@c8y/client';
import { BsModalRef, BsModalService } from 'ngx-bootstrap/modal';
import { AlertService } from '../alert/alert.service';
import { AppStateService } from '../common/ui-state.service';
import { UserPreferencesService } from '../common/user-preferences/user-preferences.service';
import { TranslateService } from '../i18n/translate.service';
export declare class UserEditModalComponent {
    modal: BsModalRef;
    user: UserService;
    ui: AppStateService;
    private auth;
    private client;
    private alert;
    private translate;
    private userPreferences;
    private modalService;
    currentUser: IUser;
    lang: string;
    changedLang: string;
    loading: boolean;
    constructor(modal: BsModalRef, user: UserService, ui: AppStateService, auth: BasicAuth, client: FetchClient, alert: AlertService, translate: TranslateService, userPreferences: UserPreferencesService, modalService: BsModalService);
    onDismiss(): void;
    onLanguage(lang: any): void;
    updateAndClose(user: any): Promise<void>;
    persistLanguage(lang: any): Promise<void>;
    private updateCredentials;
}
