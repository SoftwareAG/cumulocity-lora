import { UserService } from '@c8y/client';
import { ModalService } from '../modal/modal.service';
import { BsModalRef } from 'ngx-bootstrap/modal';
import { LoginService } from '../login/public-api';
export declare class UserTotpSetupComponent {
    private user;
    private modalService;
    private modal;
    private loginService;
    constructor(user: UserService, modalService: ModalService, modal: BsModalRef, loginService: LoginService);
    totpSetupVerified(token: any): Promise<void>;
    close(): void;
}
