import { OnChanges, SimpleChanges } from '@angular/core';
import { OptionsService } from '../../common/options.service';
export declare class AppIconComponent implements OnChanges {
    private options;
    contextPath: string;
    name: string;
    app: any;
    model: {
        appIcon: string;
        showIcon: boolean;
        iconClass: string[];
        appNameAbbr: string;
        iconStyle: {};
    };
    private c8yAppIconsList;
    private white;
    private defaultIconStyle;
    constructor(options: OptionsService);
    ngOnChanges(changes: SimpleChanges): void;
    private findStaticIcon;
    private iconFromAppDefinition;
    private iconUrlFromAppDefinition;
}
