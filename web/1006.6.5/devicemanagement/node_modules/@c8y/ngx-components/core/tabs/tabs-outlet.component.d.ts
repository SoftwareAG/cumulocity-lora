import { Renderer2, ElementRef, OnDestroy, AfterContentInit } from '@angular/core';
import { Router } from '@angular/router';
import { Tab } from './tab.model';
export declare class TabsOutletComponent implements OnDestroy, AfterContentInit {
    private router;
    private renderer;
    private el;
    tabs: any[];
    orientation: 'vertical' | 'horizontal';
    navigatorOpen: boolean;
    showLeft: boolean;
    showRight: boolean;
    readonly isNavigatorOpen: boolean;
    readonly hasTabs: number;
    readonly isHorizontal: boolean;
    readonly isVertical: boolean;
    private container;
    private leftBtn;
    private rightBtn;
    private tabsContainer;
    private scrollSubscription;
    constructor(router: Router, renderer: Renderer2, el: ElementRef);
    ngOnDestroy(): void;
    ngAfterContentInit(): void;
    isActive(tab: Tab): boolean;
    navigateTo(path: any): void;
    scroll(direction?: 'right' | 'left'): void;
    private scrollSubscribe;
    private updateScroll;
    private getDimensions;
}
