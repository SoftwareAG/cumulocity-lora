import { Injector } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { ApiService, ApiCall } from '@c8y/ngx-components/api';
import { Observable } from 'rxjs';
import { TabsService } from '../tabs/tabs.service';
import { RouterTabsResolver } from './router-tabs.resolver';
export declare class ContextRouteComponent {
    private tabsResolver;
    private tabsService;
    private route;
    private router;
    private apiService;
    private injector;
    private dataSubscription;
    private routerSubscription;
    private lastAddedTabs;
    private refreshTrigger;
    constructor(tabsResolver: RouterTabsResolver, tabsService: TabsService, route: ActivatedRoute, router: Router, apiService: ApiService, injector: Injector);
    ngOnInit(): void;
    ngOnDestroy(): void;
    refreshTabs(): void;
    updatedContext(): Observable<ApiCall>;
    private updateTabs;
    private redirectToFirstTab;
    private needsRedirect;
}
