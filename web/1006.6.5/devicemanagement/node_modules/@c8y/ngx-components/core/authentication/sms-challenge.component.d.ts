import { EventEmitter } from '@angular/core';
import { UserService, ICredentials } from '@c8y/client';
import { LoginService } from '../login/login.service';
import { AlertService } from '../alert/public-api';
export declare class SmsChallengeComponent {
    loginService: LoginService;
    private users;
    private alert;
    credentials: ICredentials;
    onCancel: EventEmitter<any>;
    model: {
        smsToken: string;
    };
    isLoading: boolean;
    private resendTfa;
    constructor(loginService: LoginService, users: UserService, alert: AlertService);
    verifyTFACode(): Promise<void>;
    resendTFASms(): Promise<void>;
    private useOAuthInternal;
    private verifyCodeWithOauth;
    private verifyCodeWithBasicAuth;
    private loginWithTFA;
}
