import { OnInit, EventEmitter } from '@angular/core';
import { NewPassword } from './password.model';
import { SystemOptionsService } from '@c8y/client';
export declare class NewPasswordComponent implements OnInit {
    private systemOptions;
    password: EventEmitter<{} | NewPassword>;
    model: any;
    enforcePasswordStrength: boolean;
    changePassword: boolean;
    constructor(systemOptions: SystemOptionsService);
    ngOnInit(): void;
    newPasswordChanged(): void;
    loadEnforcePasswordStrength(): Promise<void>;
    toggleChangePassword(): void;
}
