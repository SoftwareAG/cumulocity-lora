import { Validator, AbstractControl } from '@angular/forms';
export declare class PasswordConfirm implements Validator {
    passwordConfirm: string;
    constructor(passwordConfirm: string);
    validate(abControl: AbstractControl): {
        [key: string]: any;
    };
}
