import { Breadcrumb } from './breadcrumb.model';
export declare class BreadcrumbOutletComponent {
    showAll: boolean;
    breadcrumbs: Breadcrumb[];
    /**
     * For upgrade only. Old angularjs routes start with hash, new ones not.
     */
    normalizePath(path: any): any;
}
