import { Action, ActionFactory } from '@c8y/ngx-components';
import { ActivatedRoute, Router } from '@angular/router';
import { ContextDashboardService } from './context-dashboard.service';
import { BsModalService } from 'ngx-bootstrap/modal';
export declare class DashboardActionsFactory implements ActionFactory {
    private router;
    private contextDashboardService;
    private bsModal;
    constructor(router: Router, contextDashboardService: ContextDashboardService, bsModal: BsModalService);
    get(activatedRoute: ActivatedRoute): Action[];
    addDashboard(context: any): Promise<void>;
    private isEnabled;
}
