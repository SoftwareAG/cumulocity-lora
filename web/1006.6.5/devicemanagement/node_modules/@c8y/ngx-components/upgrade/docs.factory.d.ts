import { DocLinkFactory, DocLink } from '@c8y/ngx-components';
import { BridgeService } from './bridge.service';
export declare class Ng1DocsFactoryService implements DocLinkFactory {
    private bridge;
    private links;
    constructor(bridge: BridgeService);
    get(): Promise<DocLink[]>;
}
