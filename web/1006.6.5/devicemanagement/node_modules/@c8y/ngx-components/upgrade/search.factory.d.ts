import { Search, SearchFactory } from '@c8y/ngx-components';
import { BridgeService } from './bridge.service';
export declare class Ng1SearchFactoryService implements SearchFactory {
    private bridge;
    search: Search[];
    constructor(bridge: BridgeService);
    get(): Search[];
}
