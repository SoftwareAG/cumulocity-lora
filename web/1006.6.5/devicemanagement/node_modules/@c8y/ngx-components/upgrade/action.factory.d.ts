import { Action, ActionFactory, TabsService } from '@c8y/ngx-components';
import { BridgeService } from './bridge.service';
import { Observable } from 'rxjs';
export declare class Ng1ActionFactoryService implements ActionFactory {
    private bridge;
    private tabs;
    routeChanges$: Observable<any>;
    $location: any;
    constructor(bridge: BridgeService, tabs: TabsService);
    handleTabsRedirect(tabs: any): void;
    get(): Observable<Action>;
}
