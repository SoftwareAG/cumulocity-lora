import { NgForm } from '@angular/forms';
import { IManagedObject } from '@c8y/client';
import { AlertService, DroppedFile } from '@c8y/ngx-components';
import { BsModalRef } from 'ngx-bootstrap/modal';
import { RepositoryModal } from '../repository.model';
import { RepositoryService } from '../repository.service';
export declare class ConfigurationNewModalComponent implements RepositoryModal {
    private repositoryService;
    private bsModalRef;
    private alert;
    selected: {
        id: string;
        name: string;
    };
    versionOrName: string;
    description: string;
    binary: {
        file?: File;
        url?: string;
    };
    deviceType: string;
    pattern: string;
    filterPipe: any;
    configs: any;
    mo: Partial<IManagedObject>;
    isLoading: boolean;
    result: Promise<void>;
    private _save;
    private _cancel;
    constructor(repositoryService: RepositoryService, bsModalRef: BsModalRef, alert: AlertService);
    ngOnInit(): Promise<void>;
    cancel(): void;
    setPipe(filterStr: string): void;
    uploadFile(dropped: DroppedFile[], ngForm: NgForm): void;
    save(): Promise<void>;
}
