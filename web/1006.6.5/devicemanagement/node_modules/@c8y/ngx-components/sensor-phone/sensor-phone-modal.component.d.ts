import { AfterViewInit, ElementRef, Renderer2 } from '@angular/core';
import { BsModalRef } from 'ngx-bootstrap';
import { C8yStepper } from '@c8y/ngx-components';
import { SensorPhoneService } from './sensor-phone.service';
import { Realtime, DeviceRegistrationService } from '@c8y/client';
import { Router } from '@angular/router';
export declare class SensorPhoneModalComponent implements AfterViewInit {
    private bsModalRef;
    private sensorPhoneService;
    private renderer;
    private deviceRegistrationService;
    private realTime;
    private router;
    stepper: C8yStepper;
    container: ElementRef;
    pendingDeviceBootstrap: boolean;
    success: any;
    errorMessage: any;
    registrationData: any;
    tenantId: any;
    instance: any;
    deviceId: string;
    dashboardLink: string;
    private bootStrapObservable;
    private managedObjectObservable;
    constructor(bsModalRef: BsModalRef, sensorPhoneService: SensorPhoneService, renderer: Renderer2, deviceRegistrationService: DeviceRegistrationService, realTime: Realtime, router: Router);
    ngAfterViewInit(): void;
    bootstrapDevice(stepper: Partial<C8yStepper>): Promise<void>;
    generateDeviceId(): string;
    onDeviceStatusChanged(deviceId: any): Promise<void>;
    onManagedObjectCreated(deviceMo: any, dashboardMo: any): void;
    openDashboard(): void;
    cancel(): void;
    retry(): void;
    onFailure(): void;
    deleteDeviceRequest(): Promise<void>;
    cleanSubscription(): void;
    ngOnDestroy(): void;
}
