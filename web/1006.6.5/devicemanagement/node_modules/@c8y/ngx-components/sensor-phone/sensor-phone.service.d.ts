import { BsModalService } from 'ngx-bootstrap';
import { AppStateService } from '@c8y/ngx-components';
import { InventoryService } from '@c8y/client';
export declare class SensorPhoneService {
    private bsModalService;
    private ui;
    private inventoryService;
    readonly HTTP_STATUS_CODE: {
        OK: number;
        CREATED: number;
    };
    private domainName;
    private tenantId;
    private PHONE_GROUP_FRAGMENT;
    private readonly PHONE_GROUP_NAME;
    constructor(bsModalService: BsModalService, ui: AppStateService, inventoryService: InventoryService);
    createModal(): void;
    getHost(): string;
    getTenantName(): string;
    getTenantId(): string;
    checkPhoneGroupExists(): Promise<any>;
    addPhoneGroup(): Promise<any>;
    createPhoneGroup(): Promise<import("@c8y/client").IManagedObject>;
    generateRegistrationData(deviceId: any): string;
}
