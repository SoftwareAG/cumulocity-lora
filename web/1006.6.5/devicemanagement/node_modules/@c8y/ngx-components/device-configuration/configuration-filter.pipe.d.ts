import { PipeTransform } from '@angular/core';
export declare class ConfigurationFilterPipe implements PipeTransform {
    transform(items: any[], filterTerm: string): any;
    private filterContainString;
}
