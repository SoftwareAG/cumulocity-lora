import { EventEmitter } from '@angular/core';
import { ConfigurationListEmptyState, SupportedConfigurationItem } from './device-configuration.model';
export declare class ConfigurationListComponent {
    items: SupportedConfigurationItem[];
    itemIcon: string;
    emptyState: ConfigurationListEmptyState;
    isFilterEnabled: boolean;
    configSelected: EventEmitter<string>;
    selectedConfig: any;
    filterTerm: string;
    showConfigurationTypePreview(config: any): void;
    updatePipe(filterTerm: string): void;
}
