import { OnDestroy, OnInit } from '@angular/core';
import { ConfigurationSnapshot, DeviceConfigurationOperation } from './device-configuration.model';
import { IManagedObject, IOperation, Realtime, UserService } from '@c8y/client';
import { DeviceConfigurationService } from './device-configuration.service';
import { BsModalService } from 'ngx-bootstrap/modal';
import { AppStateService } from '@c8y/ngx-components';
export declare class ConfigurationPreviewComponent implements OnInit, OnDestroy {
    private deviceConfigurationService;
    private realtime;
    private bsModal;
    private user;
    private appState;
    device: IManagedObject;
    configurationType: string;
    configSnapshot: ConfigurationSnapshot;
    canSaveSnapshot: boolean;
    actionButtonText: string;
    actionButtonIcon: string;
    operationToTrigger: DeviceConfigurationOperation.UPLOAD_CONFIG | DeviceConfigurationOperation.DOWNLOAD_CONFIG;
    operation: IOperation;
    private operationsSubscription;
    constructor(deviceConfigurationService: DeviceConfigurationService, realtime: Realtime, bsModal: BsModalService, user: UserService, appState: AppStateService);
    ngOnInit(): Promise<void>;
    createDeviceOperation(): Promise<void>;
    showOperation(): boolean;
    showBinary(): boolean;
    isCreateOperationDisabled(): boolean;
    updateOperation(operation: IOperation): void;
    getOperation(): Promise<IOperation>;
    download(): void;
    saveToRepository(): Promise<void>;
    hasPermission(): boolean;
    ngOnDestroy(): void;
}
