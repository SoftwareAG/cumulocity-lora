import { EventEmitter } from '@angular/core';
import { LoginService } from '../login/login.service';
import { AlertService } from '../alert/alert.service';
import { ICredentials, UserService } from '@c8y/client';
export declare class ProvidePhoneNumberComponent {
    loginService: LoginService;
    alert: AlertService;
    private userService;
    credentials: ICredentials;
    onCancel: EventEmitter<any>;
    onChangeView: EventEmitter<any>;
    phoneNumber: string;
    requestInProgress: boolean;
    private sendTfa;
    constructor(loginService: LoginService, alert: AlertService, userService: UserService);
    sendTFASms(): Promise<void>;
    save(): Promise<void>;
}
