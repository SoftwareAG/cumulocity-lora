import { BsDropdownDirective } from 'ngx-bootstrap/dropdown';
import { EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
export interface Item {
    name: string;
    _selected?: boolean;
    [key: string]: any;
}
export declare type selectedFunction = (item: Item) => boolean;
export declare type selectedLabelFunction = (items: Item[]) => string;
export declare class SelectComponent implements OnChanges {
    placeholder: string;
    selectedLabel: string | selectedLabelFunction;
    applyLabel: string;
    items: Item[];
    selected: Item[] | selectedFunction;
    onChange: EventEmitter<Item[]>;
    dropdown: BsDropdownDirective;
    textFilter: string;
    labelText: string;
    isOpen: boolean;
    filteredItems: Item[];
    searchFilter: any;
    readonly sizeToShowFilter: number;
    labelsForSelectAll: any;
    showAllLabel: boolean;
    private itemsSelected;
    private stopClicks;
    preventClick(evt: any): void;
    isOpenChange(isOpen: boolean): void;
    outterSelected(item: Item): any;
    isSelected(item: Item): boolean;
    isAllItemsSelected(): boolean;
    isAllFilteredSelected(): boolean;
    applyChanges(): void;
    selectAll(checked: boolean): void;
    onChangeItem(checked: boolean, item: Item): void;
    updateFiltered(term: string): void;
    getSelectAllToggleStatus(): {
        label: any;
        checked: boolean;
        indeterminate: boolean;
    };
    ngOnChanges(changes: SimpleChanges): void;
    private updateLabel;
    private updateSelected;
    private isAllSelected;
    private getLabel;
}
