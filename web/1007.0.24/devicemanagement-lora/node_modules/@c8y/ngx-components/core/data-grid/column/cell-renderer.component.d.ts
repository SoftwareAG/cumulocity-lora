import { ChangeDetectorRef, DoCheck, Injector, OnChanges } from '@angular/core';
import { CellRendererSpec } from './cell-renderer';
export declare class CellRendererComponent implements OnChanges, DoCheck {
    private injector;
    private cd;
    spec: CellRendererSpec;
    _spec: CellRendererSpec;
    prevSpec: CellRendererSpec;
    rendererType: 'TEMPLATE' | 'COMPONENT';
    rendererInjector: Injector;
    constructor(injector: Injector, cd: ChangeDetectorRef);
    ngOnChanges(): void;
    ngDoCheck(): void;
}
