import { TenantService, UserService } from '@c8y/client';
import { AlertService } from '../alert/alert.service';
import { AppStateService } from '../common/ui-state.service';
import { BsModalService } from 'ngx-bootstrap/modal';
import { LoginService } from '../login/login.service';
import { ModalService } from '../modal/modal.service';
import { OptionsService } from '../common/options.service';
import { TranslateService } from '@ngx-translate/core';
import { UserMenuItem } from './user.model';
export declare class UserMenuOutletComponent {
    ui: AppStateService;
    private bsModalService;
    private modalService;
    private loginService;
    private translateService;
    private tenantService;
    private alertService;
    private user;
    private optionsService;
    items: UserMenuItem[];
    open: boolean;
    constructor(ui: AppStateService, bsModalService: BsModalService, modalService: ModalService, loginService: LoginService, translateService: TranslateService, tenantService: TenantService, alertService: AlertService, user: UserService, optionsService: OptionsService);
    copyIt(text: string): void;
    editUser(): Promise<void>;
    logout(): Promise<void>;
    activateSupportAccess(): Promise<void>;
    deactivateSupportAccess(): Promise<void>;
    getSortedItems(): any;
    private refreshCurrentUser;
    private byPriority;
}
