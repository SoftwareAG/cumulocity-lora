import { ViewContainerRef, AfterContentInit, OnDestroy } from '@angular/core';
import { TabsService } from './tabs.service';
import { Tab } from './tab.model';
export declare class TabComponent implements AfterContentInit, OnDestroy, Tab {
    private tabs;
    template: ViewContainerRef;
    path: string;
    label: string;
    icon: string;
    priority: number;
    orientation: 'horizontal' | 'vertical';
    constructor(tabs: TabsService);
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
}
