import { ApplicationService, IApplication } from '@c8y/client';
import { AppStateService } from '../../common/ui-state.service';
export declare class AppSwitcherComponent {
    private ui;
    private applications;
    oneCloudApps?: Array<IApplication & {
        [key: string]: any;
    }>;
    apps: Array<IApplication & {
        [key: string]: any;
    }>;
    open: boolean;
    private readonly hideCloudApps;
    private tenant;
    constructor(ui: AppStateService, applications: ApplicationService);
    isActive(app: IApplication): boolean;
    getHref(app: IApplication): string;
    private listAppsForUser;
    private filterApps;
    private filterVisible;
    private filterCloud;
    private filterDuplicates;
    private orderApps;
}
