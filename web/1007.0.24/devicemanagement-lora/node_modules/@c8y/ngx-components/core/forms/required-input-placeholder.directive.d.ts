import { ElementRef, Renderer2, AfterContentChecked } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';
export declare class RequiredInputPlaceholderDirective implements AfterContentChecked {
    private el;
    private renderer;
    private translationService;
    private shouldSkipRequiredHint;
    private placeholder;
    private req;
    constructor(el: ElementRef, renderer: Renderer2, translationService: TranslateService);
    ngAfterContentChecked(): void;
    private getTextForPlaceholder;
    private updatePlaceholder;
}
