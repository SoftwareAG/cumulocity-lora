import { EventEmitter, SimpleChanges } from '@angular/core';
export declare class ListItemCheckboxComponent {
    selected: boolean;
    onSelect: EventEmitter<boolean>;
    ngOnChanges(changes: SimpleChanges): void;
}
