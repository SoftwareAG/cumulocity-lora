import { ElementRef, EventEmitter, SimpleChanges } from '@angular/core';
export declare class ListItemRadioComponent {
    radio: ElementRef;
    selected: boolean;
    name: string;
    disabled: boolean;
    onSelect: EventEmitter<boolean>;
    ngOnChanges(changes: SimpleChanges): void;
    reset(): void;
}
