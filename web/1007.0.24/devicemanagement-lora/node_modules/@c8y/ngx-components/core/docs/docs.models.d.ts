import { InjectionToken } from '@angular/core';
import { ExtensionFactory } from '../common/public-api';
export interface DocLink {
    icon: string;
    label: string;
    url: string;
    type: 'doc' | 'quicklink';
    click?: any;
}
export declare type DocLinkFactory = ExtensionFactory<DocLink>;
export declare const HOOK_DOCS: InjectionToken<ExtensionFactory<DocLink>>;
