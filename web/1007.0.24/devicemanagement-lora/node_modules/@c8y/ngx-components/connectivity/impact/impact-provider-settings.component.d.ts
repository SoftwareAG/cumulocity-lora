import { OnInit } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import { AlertService, ModalService } from '@c8y/ngx-components';
import { ImpactConnectivityService } from './impact-connectivity.service';
import { TenantConnectionStatusResponse } from './impact.model';
export declare class ImpactProviderSettingsComponent implements OnInit {
    private impactService;
    private formBuilder;
    private modal;
    private alert;
    connectionStatus: TenantConnectionStatusResponse;
    formGroup: FormGroup;
    isEdit: boolean;
    credentialsExist: boolean;
    constructor(impactService: ImpactConnectivityService, formBuilder: FormBuilder, modal: ModalService, alert: AlertService);
    ngOnInit(): Promise<void>;
    replaceCredentials(): void;
    saveCredentials(): Promise<void>;
    deleteCredentials(): Promise<void>;
    private initForm;
    private resetForm;
    private safelyUpdateCredentials;
    private safelyDeleteCredentials;
}
