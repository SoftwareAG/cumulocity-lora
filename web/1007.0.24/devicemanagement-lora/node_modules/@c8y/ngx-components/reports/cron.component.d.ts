import { EventEmitter, OnInit } from '@angular/core';
import { EmittedCron, CronConfig, Base } from './export-schedules.interface';
import { CronService } from './cron.service';
export declare class CronComponent implements OnInit {
    private cronService;
    cronIn: string;
    emitter: EventEmitter<EmittedCron>;
    emittedCron: EmittedCron;
    base: Base;
    cronConfig: CronConfig;
    daysOfWeekPosix: any[];
    daysOfMonth: any[];
    months: any[];
    hours: any[];
    minutes: any[];
    intervals: any[];
    constructor(cronService: CronService);
    ngOnInit(): void;
    onChangeSelect(): void;
}
