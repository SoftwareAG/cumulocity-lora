import { EventEmitter } from '@angular/core';
import { IManagedObject, IOperationBulk } from '@c8y/client';
export declare class BulkOperationDetailsComponent {
    allFilterIsSelected: boolean;
    collapsed: boolean;
    device: IManagedObject;
    bulkOperation: Partial<IOperationBulk>;
    readOnly: boolean;
    showFailedOperation: EventEmitter<number>;
    scrollToRetriedFromOperation(failedOperationId: any): void;
}
