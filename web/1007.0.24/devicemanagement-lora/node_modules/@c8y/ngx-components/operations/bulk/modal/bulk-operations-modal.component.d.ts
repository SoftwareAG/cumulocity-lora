import { ComponentFactoryResolver } from '@angular/core';
import { BsModalRef } from 'ngx-bootstrap';
import { OperationType } from '../list/type-list/operation-type.model';
export declare class BulkOperationsModalComponent {
    private bsModalRef;
    private resolver;
    container: any;
    showComponent: boolean;
    constructor(bsModalRef: BsModalRef, resolver: ComponentFactoryResolver);
    cancel(): void;
    createComponent(item: OperationType): void;
}
