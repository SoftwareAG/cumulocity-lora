import { OnInit, ViewContainerRef } from '@angular/core';
import { Router } from '@angular/router';
import { AlertService } from '@c8y/ngx-components';
import { ImpactSubscriptionService } from './impact-subscription.service';
export declare class RefreshActionComponent implements OnInit {
    private vcRef;
    private router;
    private impactService;
    private alert;
    templateCopy: any;
    requestInProgress: boolean;
    constructor(vcRef: ViewContainerRef, router: Router, impactService: ImpactSubscriptionService, alert: AlertService);
    ngOnInit(): void;
    refresh(): Promise<void>;
}
