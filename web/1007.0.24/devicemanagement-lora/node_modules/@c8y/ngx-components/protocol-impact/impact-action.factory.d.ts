import { ActivatedRoute } from '@angular/router';
import { ApplicationService } from '@c8y/client';
import { ActionBarFactory, ActionBarItem } from '@c8y/ngx-components';
export declare class ImpactActionFactory implements ActionBarFactory {
    private applicationService;
    private static readonly applicationName;
    constructor(applicationService: ApplicationService);
    get(activeRoute?: ActivatedRoute): Promise<ActionBarItem[]>;
}
