import { SmsConfig } from './sms-config.model';
import { ModalService, AlertService } from '@c8y/ngx-components';
import { SmsGatewayService } from './sms-gateway.service';
export declare class SmsGatewayComponent {
    private modalService;
    private alertService;
    private smsGatewayService;
    smsConfig: SmsConfig;
    oldConfig: SmsConfig;
    readonly smsProviders: any;
    readonly pageTitle: any;
    private readonly supportedConfigOptions;
    constructor(modalService: ModalService, alertService: AlertService, smsGatewayService: SmsGatewayService);
    ngOnInit(): Promise<void>;
    getConfigurableProperties(config: any): SmsConfig;
    isSupportedProvider(config: any): any;
    saveSMSGatewayConfiguration(form: any): Promise<void>;
    deleteSMSGatewayConfiguration(): Promise<void>;
    getEmptyConfig(): SmsConfig;
}
