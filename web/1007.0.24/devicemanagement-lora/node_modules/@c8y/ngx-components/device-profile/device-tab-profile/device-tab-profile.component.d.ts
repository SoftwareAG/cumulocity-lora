import { OnDestroy, OnInit, Pipe } from '@angular/core';
import { IManagedObject, IOperation, IResultList, Realtime } from '@c8y/client';
import { ActivatedRoute } from '@angular/router';
import { DeviceProfileService } from '../device-profile.service';
import { DeviceProfile, ProfileItem } from '../device-profile.model';
export declare class DeviceTabProfileComponent implements OnInit, OnDestroy {
    private deviceProfileService;
    private route;
    private realtime;
    device: IManagedObject;
    deviceProfiles: IResultList<IManagedObject>;
    selectedProfile: Partial<DeviceProfile>;
    operation: IOperation;
    firmwareItems: ProfileItem[];
    softwareItems: ProfileItem[];
    configurationItems: ProfileItem[];
    filterPipe: Pipe;
    pattern: string;
    reloading: boolean;
    private operationsSubscription;
    constructor(deviceProfileService: DeviceProfileService, route: ActivatedRoute, realtime: Realtime);
    ngOnInit(): Promise<void>;
    getDeviceProfilesAndUpdateProfileItems(): Promise<void>;
    selectProfile(mo: DeviceProfile): void;
    createOperation(): Promise<void>;
    setPipe(filterStr: string): void;
    ngOnDestroy(): void;
    private updateProfileItems;
    private subscribeToOperations;
}
