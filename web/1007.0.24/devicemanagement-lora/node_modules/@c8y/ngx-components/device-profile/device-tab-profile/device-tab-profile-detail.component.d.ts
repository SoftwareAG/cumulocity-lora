import { ProfileItem } from '../device-profile.model';
export declare class DeviceTabProfileDetailComponent {
    sectionTitle: string;
    sectionIcon: string;
    emptyStateText: string;
    emptyStateDetails: string;
    isProfileSelected: boolean;
    isEmpty: boolean;
    items: ProfileItem[];
    showTextLabel: boolean;
}
