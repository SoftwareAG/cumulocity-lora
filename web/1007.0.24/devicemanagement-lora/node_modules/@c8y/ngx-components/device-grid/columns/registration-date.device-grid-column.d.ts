import { BaseDeviceGridColumn } from './base.device-grid-column';
export declare class RegistrationDateDeviceGridColumn extends BaseDeviceGridColumn {
    constructor();
    protected formatDate(dateToFormat: string): string;
}
