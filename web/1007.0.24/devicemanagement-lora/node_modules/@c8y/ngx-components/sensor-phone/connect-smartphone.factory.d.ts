import { DocLinkFactory, OptionsService, DocLink } from '@c8y/ngx-components';
import { SensorPhoneService } from './sensor-phone.service';
export declare class ConnectSmartphoneFactory implements DocLinkFactory {
    private optionsService;
    private sensorPhoneService;
    constructor(optionsService: OptionsService, sensorPhoneService: SensorPhoneService);
    get(): import("rxjs").Observable<DocLink[]>;
}
