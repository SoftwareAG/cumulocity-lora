import { AssetNodeService } from '../asset-node.service';
import { GroupNode } from './group-node';
export declare class GroupNodeService extends AssetNodeService {
    rootNode: GroupNode;
    draggedData: GroupNode;
    protected PAGE_SIZE: number;
    createGroupNode(config: any): GroupNode;
    createChildNode(managedObject: any): GroupNode;
    subassetsQueryFilter(): string;
    getGroupItems(moId: string): Promise<any>;
}
