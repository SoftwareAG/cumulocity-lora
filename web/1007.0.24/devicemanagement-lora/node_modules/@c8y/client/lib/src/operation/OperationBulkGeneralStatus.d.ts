/**
 * A bulk operation can have one of the following general statuses.
 */
export declare enum OperationBulkGeneralStatus {
    SCHEDULED,
    EXECUTING,
    EXECUTING_WITH_ERROR,
    CANCELED,
    FAILED,
    SUCCESSFUL,
    DELETED,
    INCORRECT
}
